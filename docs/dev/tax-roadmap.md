# 税務・会計データ整形 開発ロードマップ

> 策定: 2026-07-22。アーキテクチャ決定は [ADR-004](../adr/004-tax-logic-in-cli-exception.md)。
> フェーズ管理は [phases.md](phases.md) の Phase 6。

## 背景と期限

- 取引所側にデータが揃っているのに、税務処理のために利用者が自力でデータを
  組み立て直している（複数ユーザーが独立に指摘）
- **2027 年 2〜3 月の確定申告シーズン（2026 年分）に間に合わせる**
  - 12 月頭までに動くものを完成
  - 11 月中に実データ（年間取引）での検証を終える
  - 7 月下旬起点で残り約 4〜5 ヶ月

## 税制前提（2026 年 7 月時点）

- 金商法・資金決済法等改正法（暗号資産の金商法移管）は **2026-07-15 に成立**。
  暗号資産関連規定は公布から 1 年以内に施行（[tax-research.md](tax-research.md) §12）
- 申告分離課税（20%）の適用開始は「金商法等改正法の施行日の属する年の翌年 1/1 以後の
  譲渡」。2027 年中施行なら 2028-01-01 開始が有力だが、施行日確定までは未確定
- **2027 年 2〜3 月の申告（2026 年分）は現行制度（総合課税・原則その他雑所得）で確定**。
  本ロードマップは現行制度への対応を優先する
- 分離課税は対象が「特定暗号資産」に限定される見込み。対象外の譲渡は
  「総合課税の譲渡所得＋特則」の構造。経路別の具体分類は政省令・通達待ち
- 計算細目の税制調査は **完了**（[tax-research.md](tax-research.md) v2、2026-07-23 受領）。
  反映内容は「税制調査の反映」参照

## ユーザー層

税務ニーズは一様ではない。一つの機能で全員をカバーしようとしない。

| 層 | 特徴 | 必要な整形の深さ |
|---|---|---|
| **A** | 海外 CEX/DEX が主戦場。bitbank は円転の換金窓口 | 正規化まで（損益計算まで行うと他社分と二重計算になるため不要） |
| **B** | bitbank が主戦場（一般〜VIP/MM まで規模の幅） | bitbank 単体の損益計算まで（取得原価・実現損益） |
| **C** | 初心者・ライト層。含み益 20 万円超で心配になる層 | 概算の申告要否の目安＋専門家への誘導（断定的な税務判断はしない） |

## 整形の深さとスコープ境界

1. **生データ出力** — 取引履歴・入出金履歴の CSV/JSON 化（対応する）
2. **正規化** — ペア表記統一・タイムゾーン統一・手数料内訳分離など、
   他社データと合算しやすい共通フォーマット（対応する。A 層の主価値）
3. **損益計算** — bitbank 単体の取得原価計算・実現損益算出（対応する。B 層の核）
4. 他社データとの通算損益計算 — **スコープ外**。正規化データを既存の税務 SaaS
   （Cryptact、Gtax 等）に渡す前提
5. 申告要否・税額の確定判定 — **やらない**。概算の目安と専門家・国税庁への誘導まで

## アーキテクチャ（ADR-004）

```
[共有ロジック層 = CLI 内 tax カテゴリ]（生データ取得 → 正規化 → 損益計算）
        ↓
  A層・B層が実データで CLI Skill を使い倒す
        ↓
  手数料・信用建玉・複数ペア按分などのバグ/仕様漏れが実戦で見つかる
        ↓
  検証済みロジックを MCP Tool が CLI サブプロセス実行で利用
        ↓
  C層は自然言語で対話。Tool 側で計算を完結、LLM は説明・目安・誘導に専念
```

- 損益計算は CLI 内 `tax` カテゴリに実装（ADR-002 の例外。`paper` の前例に倣う）
- Skill は計算しない。CLI コマンドが返す確定値のみを使う（税務は間違えられない領域）
- 課税方式（総合課税/分離課税）は入口パラメータとして最初から持たせる。
  分離課税ロジックの実装は制度詳細確定後（2027 年前半に判断）
- 税務の年区切りは JST。「JST は表示・説明用のみ」規約の例外として
  `cli/date-utils.ts` に JST 年境界ヘルパーを追加する
- tax 出力に免責文言を機械可読な形で含める。C 層 MCP は read-only API キー導線
  （[botter-runbook](../botter-runbook.md) の read-only profile）を前提にする
- private GET とローカル計算のみ。trade エンドポイントには一切触れない

## Week 1 で潰す前提リスク（実機確認）

| # | 確認項目 | NG だった場合の影響 |
|---|---|---|
| 1 | **API 履歴保持期間**: trade_history / deposit_history / withdrawal_history が 2026-01-01 JST まで遡れるか | Web の取引報告書 CSV 取込へのフォールバックが必要（計画への影響が最大） |
| 2 | 信用の金利・建玉管理料が trade_history の `interest` に出るか | 控除項目が API だけで完結せず、段階 3 の仕様変更 |
| 3 | maker リベート（マイナス手数料）の出方（quote/base どちらに乗るか） | 手数料の原価算入仕様に影響 |
| 4 | 非 JPY ペア約定の有無 | 約定時点の円換算ロジックが段階 3 に必須になる |
| 5 | 出金 `fee` が `amount` 込みか別建てか | 正規化スキーマの手数料内訳分離の仕様 |
| 6 | API に存在しない課税イベントの棚卸し（貸暗号資産の利用料、キャンペーン等） | 免責明記 or Web CSV 取込のスコープ判断（Week 4 までに決定） |

### 実機確認の結果

- **#1 履歴保持期間（2026-07-22 確認・クリア）**: trade_history は **5 年以上前**の約定まで
  遡及可（5 年スパンの since/end 指定も受理）。deposit_history も同時期の入金
  （最古約定の直前 = 口座利用開始とみられる）まで遡及を確認。withdrawal_history も
  `--end` 逆方向ページングが機能。
  **ただしこれは検証口座での結果**であり、API 一般の遡及上限を確認したものではない
  （公式 docs に保持期間の記載はなく、より古い口座で打切りが起きないことは未確認）。
  「口座利用開始以来の全履歴が取れる」と一般化しないこと。
  なお **Web CSV フォールバックが不要という結論は撤回**した（実機確認 #7）。
  販売所取引は API に現れないため、CSV は取込の必須経路である
- **#2 信用取引レコード（2026-07-23 確認・クリア）**: eth_jpy の小口ロング 1 往復
  （1 日超保有・JST 0 時を 1 回またぎ）を実機で作成し、全レコードを検算一致まで解読。
  **以下の実測値は特定性を下げるため関係式に置換した**（値を丸めると恒等式の検算が
  合わなくなり、かえって証拠価値が失われるため）:
  - **判別**: trade / order とも信用は `position_side` が入る（現物はキー自体が欠落）。
    決済レコードは `side: sell` + `position_side: long`
  - **手数料**: 建て時は `fee_amount_quote: 0` のまま `fee_occurred_amount_quote` に
    発生額のみ記録され、建玉の `unrealized_fee_amount` に累積。決済時に建て分+決済分を
    合算して決済レコードの `fee_amount_quote` で一括精算
    （実測で `建て分 + 決済分 = 決済レコードの fee_amount_quote` が成立）
  - **建玉金利**: 0.04%/日を JST 0 時時点の建玉に課金（建て直後は 0、0 時またぎで 1 日分、
    24h 経過時点では増えない）。公式の信用取引ルールと一致。決済レコードの `interest` に
    原精度で精算（実測で `interest = product × 0.0004 × またいだ 0 時の回数` が成立）
  - **`profit_loss` は値幅損益から全手数料・金利を控除したネット実現損益**
    （検算: `profit_loss = 値幅損益 − Σfee − interest` が原精度で完全一致）。
    **段階 3 で fee / interest を再控除すると二重計上になる**（最重要）。
    信用の実現損益は決済レコードの `profit_loss` をそのまま採用する
  - **端数**: positions は 3 桁丸め表示、trade の fee 系は 4 桁四捨五入、
    `profit_loss` / `interest` は原精度。段階 3 は API 値を採用し、自前計算は検算
    （`interest = product × 0.0004 × またいだ 0 時の回数` 等）に限る
  - 2025-06-30 まで建玉金利 0 円キャンペーンがあったため、**2025 年分データの
    `interest: 0` は正常**（検証で異常と誤判定しない）
  - 補足: 注文照会は pair × order_id の複合キー（不一致は 50009）。
    `agents/error-catalog.json` の 50009 の記述に反映済み（Week 2 ⑤）
- **#3 手数料（2026-07-23 確認・クリア）**: maker リベート（マイナス手数料）が実在
  （btc_jpy の全約定——数百件規模——のうち maker が十数件、そのほぼ全てが負の
  `fee_amount_quote`）。
  **段階 3 は負の手数料を扱える設計が必須**（所得区分・計上方法は税制調査項目 2）。
  base 通貨建て手数料は 0 件（実測上、手数料は quote/JPY 側のみ）。現物では
  `fee_amount_quote` = `fee_occurred_amount_quote` が全件一致（docs 記述を全量追認）
- **#4 取引ペア（2026-07-23 確認）**: 検証口座の取引歴は十数ペア・全て `_jpy` 建て
  → 円換算ロジックはこの口座の検証では不要。`matic_jpy` / `rndr_jpy` の履歴が残存
  → **リネーム資産（MATIC→POL、RNDR→RENDER 等）の取得原価引継ぎマッピング**が
  新規論点（仕様検討事項）。**追確認（2026-07-24）**: `/spot/pairs` は BTC 建て 15 ペア
  （xrp/eth/ltc/bcc/mona/xlm/qtum/bat/omg/xym/link/mkr/boba/enj/matic の各 `_btc`）を
  返すが、**BTC 建てペアは新規注文停止（bitbank 社内確認では「delist 済み」）**。
  pairs はペア定義マスタとして停止済みエントリを保持し、取引可否は `is_enabled` /
  `stop_order` 系フラグで表現される。**フラグ実値は確定**（[tax-research.md](tax-research.md)
  付録E.5）: BTC 建て 15 組は `is_enabled=true` かつ `stop_order=true`（定義は有効なまま
  新規注文のみ停止）。したがって現行取引可否の判定に `is_enabled` 単独は使えない。含意:
  - 現行取引に BTC 建ては発生しない → 円換算（税制調査 §6・P-07）と暗号資産建て手数料
    （P-11）は「当年データ」の要件ではなく、**過去年からの簿価再構築**（前年末残高を
    持たない初回利用ユーザーが過去履歴を遡って取得価額を計算するケース）の要件として残る
  - Week 2 の全ペア横断取得は delist 込みの pairs マスタを回す（過去履歴の網羅に必要）。
    ここでは**取引可否で絞り込まない**（停止済みペアにも過去の約定履歴があるため）
  - 現行取引可否を判定する用途では `is_enabled && !stop_order` 相当を使う。
    上記のとおり `is_enabled` 単独では BTC 建て 15 組を「取引可」と誤判定する
  - BTC 建て約定レコードの実形状は新規約定では作れないため、過去に BTC 建て取引歴の
    ある口座のデータ提供（ヒアリング協力者・社内）で確認する
- **#5 出金 fee（2026-07-23 確認）**: crypto 出金は `amount` と `fee` が別建てで、
  **両者の合計がちょうど切りの良い額**になることから総引落 = amount + fee と推定
  （値は特定性を下げるため省略）。正規化では fee を
  別フィールドで維持。**crypto 建て出金手数料は暗号資産での支払いのため、それ自体が
  譲渡（課税イベント）になり得る** → 税制調査項目 2 の回答を待って段階 3 の扱いを決定
- **#6 データ量（2026-07-23 確認）**: btc_jpy 全期間で数百約定（5 年超）。
  個人規模の検証ではページング・性能問題なし。VIP/MM 規模はヒアリングで確認
- **#7 販売所（即時売買）は API に存在しない（2026-07-25 確認・仕様変更）**:
  `/user/spot/trade_history` が返すのは**取引所（板）取引のみ**で、販売所取引は
  注文 ID 照合で交差ゼロ。販売所は **UI CSV「売買履歴」でのみ取得可能**。
  → **UI CSV 取込が P1 から P0 へ昇格**（[tax-requirements.md](tax-requirements.md) §2.2 / §2.4）。
  これは残高突合（ガード(d)）が取込漏れを検出した実例でもあり、当初「積立」「ダスト消滅」と
  診断していた残差はすべて販売所取引だった（[tax-evidence/BALANCE_RECONCILIATION.md](tax-evidence/BALANCE_RECONCILIATION.md) 訂正履歴）
- **#8 年間取引報告書（現物）の列構成と恒等式（2026-07-26 確認）**: 実口座の年間取引報告書
  CSV を解析した。**値は持ち込まず、列構成と関係式だけを記録する**（丸めると恒等式の
  検算が合わなくなり証拠価値が失われるため、値そのものを書かない方針を採る）:
  - 符号化は **UTF-8 BOM 付き**。1 行目は氏名・発行者のメタ行で、ヘッダは 2 行目。
    → パーサはヘッダを**列名で探す**（1 行目を捨てる位置指定にすると様式変更で静かに壊れる）
  - 列は 17 本: 通貨名 / 年始数量 / JPY建て年中購入数量・購入金額 / BTC建て年中購入数量・購入金額 /
    JPY建て年中売却数量・売却金額 / BTC建て年中売却数量・売却金額 / 移入数量 / 移出数量 /
    支払手数料 / 貸出数量 / 返却数量 / 貸出損益 / 年末数量
  - **恒等式 R1（行内）**: `年末数量 = 年始数量 + 年中購入数量 − 年中売却数量 + 移入数量
    − 移出数量 − 支払手数料`。活動のある全行で厳密に成立。
    → **移出数量は手数料を含まない**（総引落 = 移出数量 + 支払手数料）。実機確認 #5 の推定と整合
  - **恒等式 R2（行間）**: `JPY 行の年中購入金額 = Σ(暗号資産行の年中売却金額)` および
    `JPY 行の年中売却金額 = Σ(暗号資産行の年中購入金額)`。厳密に成立。
    → 金額列は**グロス**（手数料控除前）で、JPY 行は暗号資産行の鏡像
  - **未確定事項#1（⑩支払手数料の丸め）への含意**: 支払手数料の列は API の
    `fee_amount_quote`（小数第 4 位丸め）より**多い桁数を持つ**。4 桁値の有限和は
    5 桁目を持てないので、**報告書は丸め前合計である可能性が高い**。ただし
    **観測は 1 口座のみで取引所への確認は取れていない**ため、既定（P-16: API 値を正）は
    変更しない。`tax verify-report` は差が「件数 × 半 ulp」以内なら丸め差と診断する。
    → 要求仕様 §9 未確定事項#1 の担当（開発／取引所）へ確認を回す
  - **BTC 建て列と貸出列が様式に存在する**（当該口座は全ゼロ）。BTC 建てペアは delist 済み
    （#4）なので当年データには出ないが過去年の再構築では出得る。貸出（貸暗号資産）は
    v2 §7 が「データソースが無い」としていたが、**年間取引報告書には年間合計が載る**
    → 貸出ユーザーへの経路が 1 本増える（実装は P2）
  - **年始数量はあるが年始「金額」は無い**。繰越簿価の代わりにはならないので、
    ガード(c)（前年繰越の確定）は引き続き必要

- **#9 販売所「売買履歴」CSV の様式（2026-07-26 確認）**: 実口座の CSV を解析。
  **値は持ち込まず列構成と性質だけを記録する**:
  - 列は 6 本: `注文ID / 通貨 / 売/買 / 数量 / 指値価格 / 売買日時`。**1 行目がそのまま
    ヘッダ**（年間取引報告書と違いメタ行が無い）。並びは新しい順で、年をまたぐ全履歴が出る
  - **手数料列が無い**（スプレッド内包）。付録E.3 のとおり。fee=0 ではなく列そのものが無い
  - **ペア列が無い**。通貨（base）だけで、クォートは JPY 固定として読むほかない
  - **約定代金の列が無い**。P-15 のとおり `数量 × 指値価格` で算出する。ただし
    **数量は 8 桁で丸められている**ため、この積は実際の約定代金と厳密には一致しない
    可能性がある（円建て注文なら代金側が丸い額のはず）。**未確定**: 正しい約定代金を
    CSV から復元できるか。差の大きさは `tax verify-report` が測る
  - **日時にタイムゾーン表記が無い**（`YYYY/M/D HH:MM:SS`）。JST 固定として読む。
    UTC として読むと年またぎの約定が前年分に落ちる（`jstDateTimeToMs`）
  - 重複排除キーは **注文ID**。取引所約定の `trade_id` とは別空間だが、`order_id` との
    交差は防御的に検査する（要求仕様 §2.4）
  - **「売買履歴」画面には 売買 / 定期購入 の 2 タブがあり、CSV ダウンロードは画面共通**。
    定期購入タブの列は `定期購入結果 / 注文ID / コイン / 数量 / 価格 / 注文日時` で
    売買タブと異なる。**定期購入も販売所なので API には現れない**（積立ユーザーの
    取込経路はここだけ）。取り違えて渡されたときに「タブが違う」と分かるよう
    `定期購入結果` 列で先に見分ける。
    **未対応・実データ待ち**: CSV 上の列名と `定期購入結果` の値域（成功 / 失敗など）が
    未確認で、失敗した注文を取り込むと取得価額が狂う。サンプルが手に入り次第サポートする

- **#10 年間取引報告書（信用）の様式（2026-07-27 確認・2025 年分）**: 実口座の CSV を解析。
  **値は持ち込まず列構成と性質だけを記録する**（#8 / #9 と同じ方針）:
  - **同じ発行元なのに現物と揃っていない**。想定で書いていたら踏んでいた差が 4 つある:

    | | 現物（#8） | 信用（#10） |
    |---|---|---|
    | BOM | あり | **なし** |
    | 改行 | CRLF | **LF・末尾改行なし** |
    | 見出しの単位注記 | なし | **`（円）` あり**（損益・手数料） |
    | メタ行のタイトル | 年間取引報告書 | 年間取引報告書**（信用取引）** |

  - 列は 5 本で**買建玉が先、売建玉が後**（現物の並びから類推すると逆になる）。
    バッチ仕様の「CSVラベル名」表どおり `年中信用取引損益（円）` / `支払手数料（円）`
  - **PR #16 の修正が無ければこのファイルは読めなかった**。`年中信用取引損益` は
    ヘッダ行を探す目印の列でもあるため、厳密一致だと `header not found` で
    **ファイル全体を拒否**していた。社内資料が食い違ったとき片方に賭けず
    両方受け付ける形にしたのが正解だった（実物は注記付きだった）
  - **列名で引く設計がそのまま効いた**。列順が想定と逆だったが実害ゼロ。
    位置で引いていたら**買建玉と売建玉が入れ替わったまま黙って通っていた**
  - 桁は小数 8 桁固定。損益は負値を取り得る（`decStr` に正値制約を付けなかった判断の裏付け）
  - **支払手数料も 8 桁**。API の `fee_amount_quote` は 4 桁丸めで、4 桁値の有限和は
    5 桁目以降を作れない → **「報告書は丸め前合計」（未確定事項#1）が信用側でも独立に再現**
  - **年末建玉は全ゼロ**だった。したがって次の 2 つは**この検体では観測できない**:
    - ショート建玉（未観測形状のまま）
    - 年またぎ建玉 → 手数料の精算/発生の判別（資料で発生ベース確定済みなので実害なし）
  - **突合そのものは未実施**。報告書 × API 集計の比較には当該口座の API 履歴が要る。
    様式検証（パーサ疎通・列対応・未知列ゼロ）までが済んだ段階

- **#11 信用の突合を実データで初実行（2026-07-28 確認・2025 年分）**: 協力者の口座で
  `tax verify-report --year=2025 --margin-csv=...` を実行した。**値は持ち込まず関係だけ記録する**:
  - 3 通貨 9 項目のうち 8 項目が許容内。**FIFO の対応付けは正しかった**
  - 唯一外れた btc の `margin_pnl` は FIFO のズレではなく**手数料の丸め**が原因。
    3 通貨とも **`報告書損益 − API損益` が `報告書手数料 − API手数料` と一致**した
    （btc は厳密一致、doge / sol は報告書側の 8 桁丸めぶん 1e-9 だけずれる）。
    代数的にも必然で、`API損益 = profit_loss + Σ手数料(4桁丸め)` かつ
    `profit_loss = 値幅 − Σ手数料(原精度) − 利息` なので、差は丸め残差そのものになる
  - → **未解決だった「報告書の FIFO 再計算と `profit_loss` の対応」はこれで解消**
  - 手数料の合計基準は**判別できず**（年末建玉ゼロ＝年またぎ無し。予想どおり）。
    ただし btc だけ精算と発生で値が分かれ、**報告書に近いのは発生ベース**だった。
    丸め誤差スケールなので決定的ではないが、社内資料の「発生ベース」と矛盾しない
  - 支払手数料の差が丸め残差ちょうどになることで、**未確定事項#1（報告書は丸め前合計）が
    信用側でも再確認**された

### この検証で見つかった実装の穴（修正済み）

1. **`margin_pnl` の許容幅がダスト固定だった**。この値は `profit_loss` に 4 桁丸めの
   手数料を足し戻して作るので、**手数料の丸め誤差をそのまま継承する**。件数比例
   （件数 × 半 ulp）にすべきところをダスト固定にしていたため、決済件数が増えた btc で
   丸めぶんを `REPORT_EXCESS` と誤診した。閾値を緩めたのではなく、理論上の上界を
   当てはめ直した（btc の差は正しい上界の 4 割程度に収まる）
2. **`margin_fee_occurred` の許容幅が件数を数え違えていた**。精算ベースは決済レコードで
   1 回だけ丸めるが、発生ベースは**建てと決済で別々に丸められる**。決済件数だけで
   上界を作ると、建玉ぶんの丸めを勘定に入れ損ねる。建て + 決済の件数に改めた
3. **ヒントが FIFO を第一候補に挙げていた**。実際の原因は手数料の丸めで、この文言のまま
   だと利用者は建玉の対応付けを疑って時間を溶かす。
   「まず `margin_fee` 行の差と見比べる」を先頭に置き、一致するなら丸め由来だと分かる形にした

## 実データ検証（2026-07-27・検証アンカー 1 の初回実施）

実口座に対して `tax reconcile` / `tax verify-report` を実行した。**値は持ち込まず、
一致・不一致の関係と性質のみを記録する**。

### 結果

- **販売所 CSV の取込で残高突合が閉じた**。btc の残差は `--brokerage-csv` 投入で
  **厳密にゼロ**になり、投入前の残差が売買履歴 CSV の当年買付数量の合計と桁まで一致した
- ダスト銘柄のうち 2 銘柄は厳密ゼロ、残る 3 銘柄は **1e-9 オーダー**まで縮んだ。
  これは CSV の数量表示が 8 桁で、実約定が 9 桁目を持つことによる丸めで、
  v2 付録E.3 の「残りも CSV の表示桁による丸め（1e-9 オーダー）で説明できた」を独立に再現している
- **年間取引報告書（現物）との突合は全項目が許容幅内で一致**。うち JPY の支払手数料を除く
  全項目は**差が厳密にゼロ**。R1 / R2 も全行 ok
- 販売所を含まない年での突合だったため、これは **API 経路だけの完全な検証**にあたる

### 未確定事項#1（⑩支払手数料の丸め基準）への決定的な材料

唯一ゼロにならなかった JPY の支払手数料について:

- 報告書の値は API の `fee_amount_quote`（小数第 4 位丸め）より**多い桁数**を持つ。
  4 桁値の有限和は 5 桁目を持てないので、**報告書は 4 桁丸め値の合計ではない**
- 差は **件数 × 半 ulp（0.5×10^-4）以内**に収まった。つまり差は
  「1 件ずつ 4 桁に丸めたこと」だけで完全に説明できる

両者を合わせると、**報告書は丸め前の合計**であると結論できる。取引所への確認は
引き続き必要だが、既定（P-16: API 値を正）を見直す判断材料としては十分。

### 未確定事項#8（未観測データ形状）の 1 つを実観測

保留リストに 1 件だけ残った行が **`status=CANCELED` の入出庫**だった。要求仕様 §9
未確定事項#8 が「未観測」として挙げていた 4 形状（ショート建玉 / 部分決済 / fiat 入庫行 /
CANCELED の crypto 出庫）のうちの 1 つにあたる。

**防御実装は正しく効いていた**。CANCELED は成立していないので残高にも税務にも載せず、
その結果として全資産の突合が閾値内に収まっている。載せていれば残差が出ていた。

ただし保留の文言が「取り込めなかった」と読める形だったため、**正常な除外なのに異常に
見える**問題があった（種別も書かれておらず、入庫か出庫かも分からなかった）。文言を
「成立していないので載せません（異常ではありません）」へ改め、種別を明記した。

### この検証で見つかった実装の穴（修正済み）

1. **ダスト閾値が全通貨一律 1e-4** だった。JPY の 1e-4 は「100 分の 1 銭」で、
   約定代金の丸め由来の残差を毎回「未取込の処分」と誤診する。通貨別既定（JPY は 1 円）を入れた
2. **`reconcile` が pending の件数しか出していなかった**。理由が見えないと動きようがないので
   理由ごと出すようにした
3. **年分の取り違えを検出できなかった**。報告書 CSV は年度情報を持たないため、
   初回の実行で前年の報告書を当年として突合し、ほぼ全項目が不一致になった。
   不一致の割合が高いときに真っ先に年分を疑わせる警告と、ヒントへの候補追加を行った

## 実データ再検証（2026-07-28・最新 main での回帰確認・実機確認 #12）

PR #17〜#27 で pnl 経路の出力が変わった（表示ガードへの打ち切り条件の追加・`source` の
2 スコープ分割・`nta_compat` を `reference` と同条件化・信用突合の許容幅とヒント）。
初回実施（07-27）はこれらのマージ前なので、検証の鎖を最新 main へつなぎ直した。
**値は持ち込まず、一致・不一致の関係と性質のみを記録する**（初回の記録形式に従う）。
事前に `npm ci` → `npx vitest run` 全緑を確認してから実データに進んだ。

### 結果: 初回と同じ関係が成立（回帰なし）

- **残高突合**: 全通貨 MATCH。btc は**厳密ゼロ**、ダスト銘柄 3 件は 1e-9 オーダー
  （閾値 1e-4 の内側）。jpy だけ 1e-3 オーダーだが**通貨別閾値（1 円）の内側**で、
  初回検証で入れた通貨別既定がそのまま効いている。`unreconcilable` ゼロ・警告ゼロ。
  保留 1 件は初回と同じ `status=CANCELED` の出庫で、改訂後の文言で出力された
- **報告書突合（現物）**: 全項目が許容幅内。`fee` 以外は差が**厳密ゼロ**、
  R1（通貨ごと）・R2-buy・R2-sell も全行 ok。`unsupported` ゼロ・未知列ゼロ。
  唯一ゼロにならない jpy の `fee` は診断が `FEE_ROUNDING` で、
  **未確定事項#1（報告書は丸め前合計）の現象が最新 main でも再現**した
- **pnl（本タスクの主目的）**: 次の 3 点をすべて満たした
  - 打ち切りの無い実行で `full_history.truncated` は false、`blocked_by` に
    打ち切り理由はゼロ件。**新しい truncated 条件による誤ブロックは無い**
  - `reference` と `nta_compat` は全銘柄で**同時に出るか同時に出ないか**のどちらかで、
    片方だけの銘柄はゼロ（契約どおり）
  - `source.year.events` は全履歴イベント数以下で、`tax events` の年別内訳の
    当年件数と完全に一致した

**`reference` の数値比較は未実施**。初回実施時の出力が保存されておらず、突き合わせる
相手が無い。報告書突合（上記 2 点目）は**独立した回帰確認**であって、これの代替にはならない
——突合が見ているのは購入・売却・手数料などの年間集計で、`reference` が持つ譲渡原価・
期末簿価・参考損益や、繰越に依存する計算は対象外だからである。

### pnl は「ガード挙動の確認」に留めた（繰越が未確定のため）

当該口座は 2020 年から履歴があり **2025 年は初年度ではない**。前年繰越 JSON が
手元に無いため `--carryover=zero` で実行した。したがって**数値は税務的に無効**であり、
確認したのは上記のガード挙動のみ（数値は参照も記録もしていない）。

この実行で見えた構造的な制約を 2 つ記録する:

- **繰越を CLI 自身で作る経路が閉じている**。翌年へ渡す `closing_qty` /
  `closing_cost_jpy` は `reference` の中にあり、`reference` はガードが通った銘柄でしか
  出ない。初年度から順に鎖を作る方式は、途中の 1 年でガードが落ちればそこで切れる
- **外部入庫のある銘柄は繰越を入れても解けない**。`--carryover=zero` 由来の違反
  （期末数量が負・取得ゼロで処分あり）とは別に、`(b) 未解決の入庫` が 4 銘柄に付いた。
  うち 1 銘柄は `(b)` 単独で止まっており、繰越とは独立の理由であることが見て取れる

→ **実口座での pnl 本番検証は「前年繰越の確定」が前提**という宿題が残る。

### ネガティブ確認（`--max-pages=1`）

全銘柄で `reference` / `nta_compat` が消え、`blocked_by` に打ち切り理由、レポート
`warnings` と stderr（`truncated data returned (MAX_PAGES)`）に警告、envelope に
`partial: true` と `meta.truncated` が出た。**このとき取得イベント数は上限なし実行と
同数だった**（＝実際には 1 件も欠けていない）が、それでもブロックしている。
ページャは「新規行ゼロのページ」でしか完了を判定しないため、上限に当たった時点では
完全性を証明できず打ち切り扱いにする（`cli/tax/import/paginate.ts`）。
**欠損の有無ではなく証明可能性で倒す**設計が実機で確認できた形になる。

### 副産物: `error-sanitize.ts` の穴（検証中に露出）

検証中に CSV パスの指定を誤ったことで露出した。当初は 2 件とも本タスクの範囲外として
記録だけ残し、その後 follow-up で個別に潰した。3 は後から見つかった同種の穴で未修正。

1. **EPERM と ENOENT を区別できない**（修正済み・PR #30）。`Cannot read CSV file: <basename>`
   に潰れるため「ファイルが無い」のか「読めない」のかが判らない。実際、macOS の TCC
   （`~/Downloads` の保護）で読めないケースの切り分けに数往復かかった。パスを出さずに
   `(file not found)` / `(permission denied)` の別だけ添える形で解消
   （errno → 文言の変換は `cli/fs-error.ts` が単一ソース）
2. **スラッシュが重なるとマスクが素通りする**（修正済み・PR #29）。`~/` の後ろに絶対パスを
   続けたときに `/Users/<user>//Users/<user>/...` という文字列が作られ、2 つ目のパスが
   basename 化されずホームディレクトリ名ごと残った。`UNIX_PATH_RE` の lookbehind
   `(?<![\w:/.~])` が直前の `/` で弾かれるため
3. **非 ASCII のパス要素でマスクが止まり、ユーザー名が残る**（未修正）。`UNIX_PATH_RE` の
   文字クラス `[a-zA-Z0-9._-]` が日本語等にマッチしないため、`/Users/<user>/確定申告/x.csv`
   は `/Users/<user>` までしか短縮されず `<user>/確定申告/x.csv` が出る。空白入りの
   `My Documents` でも同様に途中で切れる。税務用途では「確定申告」のような
   フォルダ名は普通にあり得るので、2 より発火しやすい

なお PR #24 のマスク自体は 1 の場面で実機確認できている（絶対パスが basename に短縮された）。

## 最新 main への追検証（2026-07-30・PR #28〜#31 マージ後）

実機確認 #12（07-28）の後に PR #28〜#31 がマージされたため、検証の鎖が現在の main
（`617c55e`）についているかを確認した。**結論: 手順 1〜4（reconcile / verify-report /
pnl / ネガティブ確認）の再実行は不要**で、#12 の記録がそのまま現在の main に当てはまる。
根拠は差分の中身であって「たぶん影響しない」ではない:

| #12 以降に変わった `cli/` のファイル | 変更の性質 |
|---|---|
| `error-sanitize.ts` | パスマスクの正規表現（連続スラッシュ・PR #29） |
| `fs-error.ts`（新規） | errno → 文言の変換（PR #30） |
| `carryover.ts` | **読み取り失敗時のメッセージのみ**。成功経路は無変更 |
| `parse-csv.ts` | 同上（`fail()` への抽出 + errno 付加）。decode・サイズ上限判定は無変更 |

税の計算経路（`cli/tax/engine/` / `guard/` / `ledger/` / `compat/` と
`cli/commands/tax/` 配下すべて）は**差分ゼロ**。#12 が測った関係を変え得る変更が
入っていない。事前確認として現在の main で `npm ci` → `npx vitest run` を実行し全緑
（200 ファイル / 2040 件）。

**この節に 実機確認 の採番は付けない**。実口座のデータを使っていないため、#1〜#12 と
同じ重みの確認ではない。

### 手順 5（パスマスク）だけは実機で再確認した — 唯一実装が変わった箇所なので

マスクの確認は実口座の API キーも CSV も要らない（CSV はコマンド層で読むので
API 到達前に落ちる）。4 ケースを直接実行し、**関係のみ**記録する:

| 入力パスの形 | `error` に出た形 | 判定 |
|---|---|---|
| 絶対パス（存在しない） | basename + `(file not found)` | PR #24 + PR #30 とも OK |
| 連続スラッシュ（`/a/b//a/b/x.csv`） | basename まで短縮 | **PR #29 の修正を実機で確認** |
| 非 ASCII 要素（`…/確定申告/x.csv`） | ユーザー名以降が残る | **上記 3 を実機で再現**（未修正） |
| 空白入り要素（`…/My Documents/x.csv`） | フォルダ名以降が残る | 同種。ただし**ユーザー名は落ちる** |

- `--carryover` の読み取り失敗も basename + `(file not found)` になることを確認した
  （`carryover.ts` の変更点。`fsErrorSuffix` が CSV 側と同じ文言を返している）
- `(permission denied)` の分岐は**このセッションでは確認できない**（root 実行のため
  `chmod 000` が効かず読めてしまう）。上記 1 の macOS TCC の場面で既に実機確認済み
- **上記 3 の severity に補足**: 非 ASCII は**ユーザー名まで残る**のに対し、空白入りは
  ユーザー名は basename 化で落ち、フォルダ名以降だけが残る。**前者のほうが重い**
  （同じ穴の 2 つの現れ方だが、漏れる情報の範囲が違う）

### 手順 1〜4 は未実行（このセッションに実口座の入力が無い）

`~/.bitbank/` も `.env*` も `BITBANK_*` 環境変数も無く、年間取引報告書 CSV /
売買履歴 CSV / 前年繰越 JSON も存在しない。**値を持ち込まない方針と同じ理由で、
実行していない結果を記録することもしない**ので、手順 1〜4 の再実行結果はここに書かない。
上表のとおり計算経路に差分が無いため、この時点で必要だったのは「再実行」ではなく
「再実行が要る差分が無いことの確認」だった、という整理になる。

#12 の宿題（**実口座での pnl 本番検証は前年繰越の確定が前提**）は未消化のまま残る。
これは main の移動とは独立で、前年繰越 JSON が手に入るまで解けない。

## 社内資料からの確定事項: 信用の年間取引報告書（2026-07-26 受領）

信用取引の年間取引報告書の仕様書（社内設計資料・2024-11）を受領した。**現物とは別ファイル・
別様式**で、ペア（通貨）単位に 5 列を持つ。ヘッダ（氏名・発行者）は現物と共通。

> **列順の正は実 CSV**（実機確認 #10）: `通貨名 / 年末保有中買建玉 / 年末保有中売建玉 /
> 年中信用取引損益（円）/ 支払手数料（円）` ——**買建玉が先**。
> 仕様書側は論理項目を売建玉→買建玉の順で列挙しており、**物理列順とは逆**なので、
> ここを読んで列マッピングを反転させないこと。実装契約は `MARGIN_COLUMNS`
> （`cli/tax/import-csv/margin-report-columns.ts`）で、照合は列名なので順序に依存しない。

- **損益は個別法（FIFO）・約定ベース**。2024-11-26 の国税庁への照会で「計上時点の問題も
  含めて全て約定をベースに考える」との回答を得ている。現物の総平均法・移動平均法とは
  別系統（v2 §5 / FAQ 2-13 と整合）
- **年中信用取引損益 = 値幅損益 − 利息**。**取引手数料は控除しない**。手数料は
  「支払手数料」列に分けて載り、申告時にユーザーが現物の取引手数料と合算して
  国税庁計算書の「手数料等」欄へ入れる（取引手数料は消費税の課税対象という整理）
- **年末建玉は法人向け**（みなし決済損益額。FAQ 3-1-13）。個人の申告には不要で、
  保有中建玉の評価（未実現損益）には言及しない方針
- 桁は小数点以下 8 桁・四捨五入。信用取引の開始日は 2024-11-11

### P-06 への含意（受領仕様との矛盾の報告）

[tax-research.md](tax-research.md) v2 §5-4（P-06）は「年間取引報告書の⑨損益合計と
**定義・対象期間が一致し**、未決済建玉の評価損益や現物損益が混入していないことを
ツールが保証できる場合に限り、計算書互換出力ではネット額（差益/差損）を使用できる」と
条件付きで定めている。受領資料により、**この条件は満たされない**ことが判明した:

| | 定義 |
|---|---|
| 報告書「年中信用取引損益」 | 値幅損益 − 利息 |
| API `profit_loss` | 値幅損益 − Σ手数料 − 利息（実機確認 #2 で原精度の検算一致） |

したがって **報告書の損益 = `profit_loss` + Σ手数料** であり、`profit_loss` をそのまま
差益/差損欄に置くと報告書と手数料ぶんずれる。さらに、その手数料は計算書のどの欄にも
現れなくなる（現物の手数料と足せない）。

実装は手数料を**足し戻して**差益/差損に、手数料は必要経費（手数料等）に分ける形へ
変更した（`cli/tax/ledger/margin-entries.ts`）。**所得の合計は変わらない**
（収入 − 経費は同値）が、報告書・計算書との行の対応が変わる。

**P-06 の【方針】そのものは変更していない**（条件の当てはめが変わっただけ）。
受領仕様（tax-research.md）は実装側で書き換えず、本書に記録している。★監修時に
「ネット額不可・分解必須」で確定させる材料として添える。

なお「fee / interest を再控除しない」という規律（実機確認 #2 の最重要事項）は維持している。
実装がやっているのは控除の**取り消し**であって、二重控除ではない。

### 追加受領（2026-07-27）: バッチ仕様書・要件定義

報告書を生成するバッチ（`annual_margin_trade_report_job`）の仕様書と要件定義を受領し、
**「未解決」に挙げていた 2 件がどちらも解決した**。実データ収集で解こうとしていたが、
そもそも仕様の問題であって、仕様書に答えがあった（データからの逆算は 1 サンプルでは
偶然一致し得るぶん、証拠としても劣る）。

- **支払手数料は発生ベースで確定**。要件定義が「信用取引における**新規建・決済時に
  発生した**取引手数料の総和」と定義している。精算ベース（`fee_amount_quote`）だと
  建て時レコードが 0 になり「新規建時に発生した手数料」が成立しないので、
  `fee_occurred_amount_quote` 側にあたる。総額はどちらでも同じで、**年をまたぐ建玉が
  あるときだけ計上年が変わる**。`verify-report` が `margin_fee` / `margin_fee_occurred` を
  両方出す設計はそのまま残す（実データでの追認に使う）
- **報告書は取引所の `profit_loss` を使わず、生約定から FIFO を再計算している**。
  バッチは `core_user_spot_trades` の amount / price / interest から算出する。
  計算式も明示された: `long損益 = 決済価格 − 建値` / `short損益 = 建値 − 決済価格`、
  そこから利息を引く。**手数料は引かない** → P-06 の対応（`profit_loss` に手数料を
  足し戻して報告書定義へ揃える）が正しいことの裏付け
- **年末建玉の抽出元**は `core_user_margin_position_day_begin`（`day = end_date` の
  スナップショット）。約定履歴からは復元できないので、突合せず `unsupported` に
  出す現行の扱いで正しい

### 判明した実装の穴: CSV 見出しの単位注記（修正済み）

バッチ仕様の「CSVラベル名」表は `年中信用取引損益（円）` / `支払手数料（円）` と
**注記付き**だが、要件定義の「出力項目」リストは注記なしで、**2 資料が食い違っている**。

パーサは列名の厳密一致で引いていたため、注記付きのファイルを渡すと
`header not found` で**ファイル全体が読めなかった**（`年中信用取引損益` は
ヘッダ行を探す目印の列でもあるため）。正しいファイルに「様式が違う」と言う挙動になる。

末尾の単位注記を落として照合する形に修正した。厳密一致を先に試し、落とした形が
**宣言済みの列名と完全一致するときだけ**採用するので別の列には化けない。
`（税抜）/（税込）` のように落とすと衝突する様式は、既存の重複検出が人に判断を返す。

### 要確認（取引所側）: 支払手数料の抽出ウィンドウ

バッチのパラメータは `begin_date` の既定が**信用取引開始日 `2024-11-11`**（対象年の
1/1 ではない）、`end_date` が `year + 1` 年の 1/1。損益は FIFO なので建値のキューを
積むのに全履歴が要る——ここは当然。

一方**支払手数料は「`core_user_spot_trades.fee` の合計値」としか書かれていない**。
抽出したデータセット全体の単純和だとすると:

| 対象年 | ウィンドウ | fee の中身 |
|---|---|---|
| 2024 年分 | 2024-11-11 〜 2025-01-01 | 2024 年分のみ ✓ |
| 2025 年分 | 2024-11-11 〜 2026-01-01 | **2024 + 2025** ✗ |

初年度はウィンドウが偶然一致して正しく、**2025 年分から前年ぶんが混入し得る**。
資料が処理概要のサマリなので実装では対象年で絞っている可能性は十分あるが、
文面上は読み取れない。→ 取引所側へ確認を回す。
なお `tax verify-report --margin-csv` は混入していれば `margin_fee` を
`REPORT_EXCESS` に立てるので、実データがあれば検出できる。

### アンカー 2（国税庁計算書互換モード）への入力

同じ資料に、互換モードの設計を決める材料が含まれていた。

- 国税庁が「**暗号資産交換業者から送付される年間取引報告書を利用して計算する場合には、
  「総平均法用」をご使用ください**」と明記している。互換モードは**総平均法用シートを主**に
  すべきで、既定 `DEFAULT_METHOD = "total-average"` と整合する
- 信用の転記先は「5 暗号資産の所得金額の計算」の**収入金額 / 信用・証拠金（差益）**と
  **必要経費 / 信用・証拠金（差損）**。`cli/tax/ledger/margin-entries.ts` が差益・差損と
  手数料を分けて出しているのと噛み合う
  （※資料は必要経費側も「差益」と書いているが、文脈上「差損」の誤記と読める。要確認）

### 残っている実データの用途（2 点消化・1 点は別検体待ち）

2026-07-27 に 2025 年分の実 CSV を入手し（実機確認 #10）、翌 28 日に協力者の口座で
突合まで実行した（実機確認 #11）。当初挙げていた 3 点の消化状況:

1. ~~**実ファイルの物理様式**~~ → **完了**（#10）。現物と 4 点違っていた。
   特に見出しの `（円）` は **PR #16 の修正が無ければファイル全体を拒否していた**
2. ~~**突合が実際に通るか**~~ → **完了**（#11）。9 項目中 8 項目が許容内で、
   外れた 1 項目も原因を特定し実装を修正した。**信用の突合は机上を脱した**
3. **手数料ウィンドウの疑問** → **未消化**。この検体は年末建玉が全ゼロで、
   2024 年に建てて 2025 年に決済した建玉が無い。混入があっても差として現れない

**まだ観測できていない形状**（別検体が要る）:

- **ショート建玉** — この検体は売建玉が全ゼロ
- **年末に持ち越した建玉** — 全ゼロ。年末建玉の列に値が入るケースを一度も見ていない
- **年をまたぐ建玉** — 上記 3 の判別に要る（手数料基準そのものは資料で確定済みなので、
  これは「実装が仕様どおりか」の確認用）

## 免責の修正: 「単一取引所だから計算できない」という言い切り（2026-07-27）

POSITIONING が「単一取引所のデータのみからは正確な譲渡原価を**原理的に**計算できません」と
無条件に述べていた。これは**誤り**で、しかも**自分のガードを否定していた**。

限界が生じるのは「単一取引所だから」ではなく「**口座外に同一銘柄の保有・取引があるとき**」。
bitbank だけで完結している銘柄なら、bitbank のデータが平均法のいう「全体」そのものなので
譲渡原価は正しく出る。まさにそれを識別するために v2 §1.2 の表示ガード (a)
アテステーション（`--attest` =「この銘柄を bitbank 口座の外で保有・売買していない」）が
存在する。無条件に否定すると、ガードを通した銘柄の数値まで無効に見え、
**bitbank だけで取引している利用者 —— 要求仕様 §1.2 の主ターゲットである B 層 —— を
蔑ろにする**文言になっていた。

v2 との齟齬ではない。v2 §1.2 がガードを定義している時点で計算が成立する条件を
認めており、**転記の過程で条件が落ちていた**という整理。文言を条件付きへ改め、
ガードが通った銘柄については数値が有効であることを述べる形にした。受領仕様
（[tax-research.md](tax-research.md)）は**書き換えていない**（P-06 と同じ扱い）。

なお呼称は **「税計算用参考データ」のまま**とする。一度「税務計算用データ」へ
変更したが差し戻した。「参考データ」単独だと品質への留保に読めるが、
**「税計算用」という修飾が付けば「税計算に使う参考資料」と読める**ため、
言い換える必要がない（製品判断・2026-07-27）。

## 課税方式パラメータ（2026-07-27 実装）

ADR-004 が「入口パラメータとして最初から持たせる。分離課税の実装そのものは制度詳細が
確定するまで着手しない」としていた項目。v2 §12 の「将来対応フック: 課税年度→課税方式
マッピングテーブル」にあたる。

**ユーザーが選ぶ値にしていない**のが設計の要点。どの課税方式が適用されるかは譲渡の年で
法律上決まるので、年→方式のテーブル（`cli/tax/taxation.ts`）を単一ソースにし、
`--taxation` は**テーブルと食い違っていないかの確認**にしか使えない。自由に選ばせると
総合課税の前提で計算した数値に分離課税のラベルが付き得る。課税方式が変われば損益通算の
範囲・損失の繰越・税率がすべて変わるため、ラベルの取り違えは数値そのものの誤りと同じ重さになる。

| 年 | 方式 | certainty | 根拠 |
|---|---|---|---|
| 〜2026 | comprehensive | `settled` | 適用開始は「施行日の属する年の**翌年** 1/1 以後」。最速（2026 年中施行）でも 2027-01-01 開始なので 2026 年分はどう転んでも総合課税 |
| 2027 | comprehensive | `projected` | 施行が 2026 年中だった場合は分離課税があり得る。施行日未確定 |
| 2028〜 | **決めない** | — | 施行日・対象範囲（特定暗号資産の定義）とも未確定。推測せずエラーにする |

- 決まらない年・食い違う指定・未知の値はいずれも **API を叩く前に**エラーにする
  （確定できない年で認証・レート制限を消費しない）
- レポートに `taxation: { mode, certainty, basis }` を常時出し、**数値の意味を固定する**
- `resolveTaxation` は `mode !== comprehensive` を弾く分岐を持つ。テーブルが今は
  `separate` を返さないので現時点では到達しないが、**テーブルに足した瞬間に効く安全網**
  （ロジック未実装のまま数値が出るのを防ぐ）

### あわせて修正: 免責の対象年ハードコード

`FUTURE_REGIME` が「本レポートが対象とする**2026年分**には適用されません」と年を
固定で埋めていた。`--year=2027` を渡すと事実と食い違う文言が出る。対象年を引数で
受ける関数に変え、`projected` の年は「適用されるかは施行日により確定します。本レポートは
総合課税を前提に計算しています」と言い切らない表現に分けた。

## 税制調査の反映（2026-07-23 受領・v2）

調査・仕様の本体は [tax-research.md](tax-research.md)（ルールを【確定】【互換】【方針】の
3 層タグで管理。【方針】は付録 B に集約し**監修要否の列で ★/△/− に切り分け**）。段階 2・3 の仕様に直結する
決定と、実機確認との突合結果:

- **位置づけは B 案**: 出力は「bitbank 口座における取引集計・税計算用参考データ」。
  平均法は全取引所・全ウォレット横断で計算するものなので、単一取引所のデータだけでは
  正確な譲渡原価を原理的に計算できない（FAQ 2-8）。参考損益は**表示ガード**
  （ユーザーのアテステーション・未解決入庫なし・前年末残高確定）成立時のみ数値表示（§1.2）。
  ロードマップの「B 層向け損益計算」は以後「参考損益」と呼ぶ
- **信用取引**: FAQ 2-13（個別法・決済年帰属・金利は決済損益へ織込み）は実機確認 #2 の
  bitbank `profit_loss`（ネット値）と整合。ただしネット額の利用は条件付き（P-06）で、
  売買代金・金利・管理料・手数料の**分解明細を常時併記**する。分解は #2 で解読済みの
  `fee_occurred_amount_quote` / `interest` から機械生成できる
- **マイナス手数料**: 受取時に収入計上（リベート収入）の単一標準・切替不可（P-04）。
  実測（#3）では bitbank のリベートは JPY 建てのため簿価計算への影響なし
- **端数処理**: 内部は Decimal 非丸め・**浮動小数点禁止**（P-02）。丸めは出力層と
  Excel 互換モードに隔離。→ **実装論点**: JS の number は使えないため、外部依存最小
  方針との調整（decimal ライブラリ導入 or BigInt 固定小数点の自作）を **Week 6 冒頭に
  ADR 化**する。不変条件テスト I1〜I4（§3）を必須テストにする
- **認識基準**: 約定日時ベース・JST 年分判定・年度内固定（P-09）。正規化は
  `ts_utc` / `ts_jst` を二重保存（§8.5）— Week 2 の JST 年境界ヘルパーの仕様が確定
- **正規化スキーマ**: 調査 §13 の Event スキーマ（kind / costbasis_provenance /
  入庫理由 enum / `UNRESOLVED_TRANSFER` フラグ、A 案=全口座統合へ拡張可能）を
  Week 4 の仕様の土台にする
- **検証アンカー追加**: FAQ 公式設例のゴールデンテスト（2-4 総平均 譲渡原価 3,106,000 /
  移動平均 3,080,200 / 2-8 所得金額 2,189,000 ほか）を必須テストに昇格。国税庁計算書
  との一致は xlsx 数式解析後の「互換モード」として 2 段構え（§11）。FAQ は例年 12 月
  下旬改訂のため、毎年 1 月のリリース手順に差分確認を組み込む
- **C 層向け文言**: 20 万円ルール免責は §10 v2 テンプレートを採用（住民税・
  判定除外所得の但し書き込み）。所得区分アラート（収入 300 万円超で警告・判定はしない、
  P-12）を実装する
- **5% ルール**: 自動適用禁止。ユーザー明示選択＋監査ログのみ（P-10）

## 週次実行計画

> **読み方の注意**: 週の完了は「週表に印が付いているか」ではなく**成果物が存在するか**で判断する。
> 成果物列が埋まっていればその週の目的は果たされている（週表の行だけを見て
> 「未着手」と判断しない。実際に一度誤読が起きた）。

| 週 | リリース内容 | 成果物（実体） | 状態 |
|---|---|---|---|
| Week 1 | ヒアリング項目案の確定＋A/B 層への打診開始。生データ取得 API の網羅化に着手。**実機確認 6 項目**。ADR-004 | 本書「実機確認の結果」#1〜#6 / [ADR-004](../adr/004-tax-logic-in-cli-exception.md) | **完了** |
| Week 2 | 生データ取得の網羅化を完成・リリース（trade-history 全ペア横断 / 入出金 `--all` ページング / withdrawal 全 asset 横断 / JST 年境界ヘルパー） | `cli/date-utils.ts` の JST ヘルパー / `deposit-history-all` / `withdrawal-history-all` / `trade-history-all-pairs`（PR #1〜#4 マージ済み） | **完了** |
| Week 3 | ヒアリング実施（A/B 層）。並行してトラック 3（投資オンボーディング/ツール最適化）の設計に着手 | **[tax-requirements.md](tax-requirements.md) §1.2「主ターゲット」+ B 層規模感テーブル** — 中口裁量勢が主ターゲット（実需 3 万件規模）、上位は 10 万〜12 万件、初心者、法人対象外。NFR の 10^5 件と §1.3 の非目標はここから直接導出 | **完了**（B 層規模感データも 2026-07-26 に取得・反映済み） |
| Week 4 | ヒアリング結果を反映し、正規化ロジック（段階 2）の仕様確定・実装。**Cryptact/Gtax 直接出力 vs 汎用フォーマットを決定**。API 外課税イベントの扱いも確定 | 仕様: [tax-requirements.md](tax-requirements.md) / [tax-p0-design.md](tax-p0-design.md)。実装: `cli/tax/import/`・`ledger/`（P0-1） | **完了**（出力形式は**汎用フォーマット**を採用。SaaS 個別フォーマットは検証アンカー 3 の参考降格に合わせて P2） |
| Week 5 | 正規化を CLI Skill としてリリース、A 層向け先行公開。CLAUDE.md / commands.md へ tax カテゴリ・例外条項追記 | `skills/tax-report/`（SKILL.md + 年間取引報告書ガイド）/ CLAUDE.md・commands.md の tax 節 | **完了** |
| Week 6 | 損益計算（段階 3）実装: 総平均法/移動平均法・手数料原価算入・信用（金利/建玉管理料）・端数処理・課税方式パラメータ | 数値基盤 [ADR-005](../adr/005-tax-exact-rational-arithmetic.md) / `cli/tax/ratio.ts` / `ratio-decimal.ts`。エンジン `cli/tax/engine/`（総平均法・移動平均法・不変条件 I1〜I4・処分上限）+ `cli/tax/guard/reference-pnl.ts`（表示ガード a〜d）+ `golden-nta.test.ts`（FAQ 設例）+ `cli/tax/taxation.ts`（課税年度 → 課税方式） | **完了** |
| Week 7 | 損益計算を CLI Skill に統合、B 層（VIP/MM 含む）先行公開。**検証アンカーとの突合開始** | `bitbank tax verify-report`（アンカー 1 の自動突合）/ `skills/tax-report` Step 3〜4 / `cli/tax/compat/nta-sheet.ts`（アンカー 2 の互換モード） | **完了**（アンカー 1 は実口座で検証済み・下記「実データ検証」。アンカー 2 は下記「検証アンカー 2 の達成」） |

### スコープ外へ移した週（2026-07-28・製品判断）

**本ロードマップの完了条件は「CLI で税計算が機能することの確認」まで**とする。
当初 Week 8 以降に置いていた次の 2 つは、そこから先の話なので本書の対象から外す。

| もとの週 | 内容 | 移した理由 |
|---|---|---|
| Week 8〜9 | MCP Tool 化（CLI サブプロセス方式・対話フロー） | CLI が正しいことの確認が先。Tool 化はその後の配布・体験の話で、完了条件に含めない |
| Week 10〜 | 12 月の本番検証に向けたバグ修正・精度向上 | 残高突合と年間取引報告書突合（現物・信用）は 7 月中に前倒しで実施済み（実機確認 #8〜#12）。通常の改修サイクルに吸収できるのはこの範囲で、**`pnl` の本番数値検証は前年繰越の確定が前提のまま残っている**（上記「実データ再検証」節） |

ADR-004 のアーキテクチャ（CLI → MCP Tool → C 層）自体は変えていない。
**順序の話であって、方針の変更ではない**。着手する段になったら本書とは別に管理する。

### 検証アンカー 2 の達成: 国税庁計算書 互換モード（2026-07-28）

`NTA_SHEET_2025_12` 互換モードを実装した（`cli/tax/compat/nta-sheet.ts`）。
これで**必須アンカーが 2 本とも埋まった**。

既定の計算（ADR-005: 内部非丸め）は変えていない。付録D.6 のとおり、レポートに
`nta_compat` 欄を足して**併記**する形にした（切替式にすると、どちらの数値を見ているか
分からなくなる）。ガードが `reference` を出す条件と揃えてあり、ガードが止めた銘柄に
互換値だけ出ることはない。

**丸めの位置が方式で違う**のがこの実装の本体:

| 方式 | 丸める場所 |
|---|---|
| 総平均法 | 単価・原価・残高は**非丸め**。最終の**収入計を切捨て（AV52）・必要経費計を切上げ（AV53）**。所得は**丸めた両者の差**（丸め前の差ではない） |
| 移動平均法 | **売却の都度、残高価額を切上げ**。譲渡原価はそこからの差引で、集計段階の追加丸めは無い |

移動平均法は後処理では済まず、**漸化式が既定エンジンと違う**（既定は `cost -= cogs`、
計算書は `cost = ceil(単価 × 残数量)`）。そのため互換モード側で回し直している。

繰越精度は **DISPLAY**（表示書式 `#,##0` = 四捨五入）を採用。手で計算書へ書き写す人と
同じ値が翌年の (B) 欄へ渡る。

**テストで固定したこと**: 公式設例（FAQ 2-4 / 2-8）の再現に加えて、
**既定と丸め位置が違うこと**を別ケースで検査している。設例は値がすべて割り切れるので、
互換モードを実装せず既定値を返しても設例テストは通ってしまうため。

### 現行提供が無い経路の優先度（2026-07-28・製品判断）

次の 3 つは**取引所が現在提供していない**ので、完全対応の優先度を下げる。

| 経路 | 現状 |
|---|---|
| **BTC 建て約定**（`TRADE_EXCHANGE` / 非 JPY クォート） | BTC 建て 15 ペアは delist 済み（実機確認 #4）。現行取引では発生しない |
| **貸出**（貸暗号資産） | 履歴 API が存在しない。年間取引報告書に年間合計だけ載る |
| **暗号資産建て手数料**（P-11） | 実測で 0 件（実機確認 #3。手数料は quote/JPY 側のみ） |

**下げてよい理由は「防御が先に入っているから」**。いずれも**黙って誤計算する経路が無い**:

- 非 JPY クォートは取込時に `NON_JPY_QUOTE` + `NO_RATE` を立て、
  **表示ガードが当該銘柄の参考損益をブロックする**（`guard/reference-pnl.ts`:
  「(b) 非 JPY クォートの約定があります」）。突合・残高再構築からも除外され、警告に出る
- `TRADE_EXCHANGE` は仕訳化せず `deferred` に積み、ガードが止める（`ledger/from-events.ts`）
- 貸出は報告書側に値があれば `unsupported` として出し、
  「当 CLI が API から再現できない列（BTC 建て / 貸出）に値があります」と警告する

つまり**未対応であることが利用者に伝わる**状態で、数値の正しさは損なわれない。
完全対応は「精度の向上」ではなく「対応範囲の拡大」にあたるので、後回しにできる。

**ただし消えたわけではない**。過去に BTC 建て取引や貸出をしていた口座が
**過去年から簿価を再構築する**ケースでは出得る（実機確認 #4 の含意）。
そのとき参考損益はブロックされるので、取引集計だけを出して手動調整に回す運用になる。

**クリティカルパスは実装に戻った**（2026-07-27 更新）: 付録B の【方針】の税務監修は
**ブロッカーから外す**という製品判断が出た（あってもよいが、待たない）。根拠は、
製品がそもそも監修なしで成立する設計になっていること:

- 位置づけが B 案（「取引集計・税計算用参考データ」）で、出すのは所得金額ではない
- 参考損益は表示ガード (a)〜(d) が全部揃った銘柄でしか数値を出さない
- 【方針】項目はレポート上「本ツールの処理方針」として明示される
- 本ツール自体がベータ版で、README に免責事項一式がある

つまり「これが正解です」ではなく「こういう方針で計算しました」と出す構造なので、
監修の不在と矛盾しない。監修があれば【方針】を【確定】へ格上げできる、という位置づけになる。
**免責文言も現状で十分**という判断が出ているため、法務レビューも同様にブロッカーではない
（社外公開の範囲を広げる段階で再検討する）。

これで**外部入力待ちがゼロ**になり、残るのは実装だけ。12 月完成に対して余裕が出る。

以下は監修に出す場合の切り分け（**記録として残す**）。付録B に監修要否の列が入り、
**★（税務判断が必要）11 件**（P-04 / P-05 / P-06 / P-07 / P-08 / P-09 / P-10 / P-11 / P-13 / P-14 / P-18）・
**△（軽微・確認のみ）5 件**（P-01 / P-03 / P-12 / P-16 / P-19）・
**−（技術判断・監修不要）3 件**（P-02 / P-15 / P-17）に切り分けられた。
**★を先に出し △ は補足質問として添える**運用でクリティカルパスを短縮する。

## ヒアリング設計（Week 1 確定 → Week 3 実施 → **要求仕様 §1.2 へ反映済み**）

> **実施済み**。結果は [tax-requirements.md](tax-requirements.md) §1.2 に「主ターゲット
> （営業ヒアリングより）」として反映されている。以下は当初の設計項目（記録として残す）。

- **A 層**: 使っている税務 SaaS（Cryptact / Gtax / 税理士渡し / 自作）と、
  そこへ渡すのに必要なフォーマット。bitbank 分を合算するときに今困っている点
- **B 層**: 法人か個人か（法人は期末時価評価が絡み損益計算の前提が変わる）、
  年間約定件数の規模（`trade-history --all` の安全弁は 100 万件）、
  データ取得時のレート制限の体感、信用取引の利用有無
- **C 層**: 既存プロファイルに近いユーザーを想定、追加ヒアリングは優先度低

## 検証アンカー（11 月実データ検証の合否基準）

同一の年間実データに対して、突合で差分ゼロ（または差分の原因を説明可能）であること。
**必須と参考を分ける**（2026-07-26 見直し）。当初は 3 つ全部を合否基準に並べていたが、
外部サービスの契約・利用者確保という**実装の正しさと無関係な事情**が検証全体の
ブロッカーになってしまうため、位置づけを分離した。

| # | アンカー | 位置づけ | 根拠 |
|---|---|---|---|
| 1 | bitbank 公式の年間取引報告書（**現物・信用の 2 本**） | **必須** | 取引所自身が発行する当年フローの全量。取得コストはユーザーの CSV ダウンロードで済み、`bitbank tax verify-report --csv / --margin-csv` で自動突合できる。現物の購入・売却の差は**販売所が第一候補**だが、履歴の打ち切り・未対応列（BTC 建て / 貸出）・年分境界・重複排除でも生じるので、`warnings` / `unsupported` を確認したうえで原因を切り分ける |
| 2 | 国税庁の暗号資産の計算書（総平均法・移動平均法の様式） | **必須** | 計算ロジックそのものの基準。**達成済み**: FAQ 設例のゴールデンテスト（`golden-nta.test.ts`）+ 互換モード `NTA_SHEET_2025_12`（`cli/tax/compat/nta-sheet.ts`。レポートの `nta_compat` 欄） |
| 3 | 正規化データを税務 SaaS（Cryptact / Gtax）に投入した損益計算結果 | **参考**（実施できれば実施） | 他社実装との相互検証であって正しさの定義ではない。差が出ても「どちらが正か」を決める根拠にならず、突合には SaaS 側の課金・仕様理解が要る。利用者も限定的 |

**11 月検証の主軸はアンカー 1 に置く**。アンカー 3 は差分が出たときの原因分析の
材料として扱い、合否は判定しない。

## 外部入力待ち・残タスク

- **税制調査**: 完了（2026-07-23 受領、[tax-research.md](tax-research.md) v2）。
  同書の残タスクを引き継ぐ:
  1. ~~国税庁計算書 xlsx の数式解析~~ → **完了**（2026-07-23、tax-research.md 付録 D
     `NTA_SHEET_2025_12`）。総平均法は E/G/I とも非丸め・最終段のみ収入切捨て/経費切上げ、
     移動平均法は売却の都度残高価額を円未満切上げ。**内部 Decimal 非丸め方針（P-02）と
     総平均法は完全一致**し、互換モードの実装コストは小さい。
     残: FAQ 設例値での検算（Week 6 ゴールデンテスト）
  2. ~~付録 B（【方針】台帳 **P-01〜P-19**）の税務監修~~ → **ブロッカーから外した**
     （2026-07-27・製品判断。上記「クリティカルパス」参照）。出す場合は ★11 件を先行、
     △5 件は補足、−3 件（P-02 / P-15 / P-17）は技術判断のため対象外
  3. FAQ 2-2 の令和 6 年 12 月版との差分確認（毎年 1 月の FAQ 改訂差分確認に合流させる）
  4. ~~免責文言一式の法務レビュー~~ → **現状の免責で十分**という判断（2026-07-27）。
     README にベータ版・自己責任・免責事項一式があり、tax 側も `disclaimers()` が
     位置づけ・スコープ外・合算注記・20 万円ルール・制度改正・丸めを常時出す。
     社外公開の範囲を広げる段階で再検討する
- **実機確認**: #1〜#6 完了。`/spot/pairs`（ペア定義マスタ）は BTC 建て 15 本を返すが
  全て delist 済み（取引不可）＝現行の取引可能ペアは全て JPY 建て（実機確認 #4）
- ~~**B 層ヒアリング（残り分）**~~ → **2026-07-26 クローズ（取得済み）**。年間約定件数の桁・
  法人比率・税務処理の現状ワークフローの 3 項目とも取得済みで、
  [tax-requirements.md](tax-requirements.md) §1.2 に表として反映した。
  10^5 は裏付けられ、10^6 は設計余裕として妥当。法人比率 2〜3 割は「大口の主体は個人」の
  観測事実であって、**§1.3 の法人除外の根拠ではない**（除外は期末時価評価により損益計算の
  前提が変わるためのスコープ判断で、比率が逆でも変わらない）。
  なお「Cryptact 等が数万件規模で落ちる」は**大量件数の正確処理が差別化点**であることの
  直接の裏付けで、NFR は「達成目標」ではなく**プロダクトの中核価値**に位置づけが変わる

## 実装状況・引き継ぎ（2026-07-24 更新）

> **運用フロー（確立済み）**: 個人フォーク `tjackiet/bitbank-lab-cli`（bitbankinc の
> true fork）の **main に蓄積**する。作業は main から切ったブランチ →
> **base=main の PR** → CodeRabbit レビュー → CI green → merge commit で main へ。
> CodeRabbit は base が default branch（main）の PR しか自動レビューしない
> （他ブランチ base では Review skipped）。**PR は必ず最初から base=main で作る**こと。
> base の変更（`edited` イベント）自体は workflow を発火させないため、branch フィルタ
> 付き workflow が走るのは次の push（`synchronize`）以降になり、CodeRabbit の自動
> レビューも base 変更だけでは始まらない（`@coderabbitai review` コメントか push が
> 必要）。会社リポは着地先（fork main → bitbankinc の PR）に徹する。
> 新セッションはまず本節・[tax-requirements.md](tax-requirements.md)（要求仕様）・
> [tax-research.md](tax-research.md)（税務仕様 v2.1。**矛盾時はこちらが正**）・
> [tax-p0-design.md](tax-p0-design.md)（P0 設計メモ・要判断リスト）・
> [ADR-004](../adr/004-tax-logic-in-cli-exception.md)（税務計算を CLI に置く例外）・
> [ADR-005](../adr/005-tax-exact-rational-arithmetic.md)（**数値基盤の契約**。厳密有理数と
> 「丸めは境界で 1 回だけ」の原則。engine 実装前に必読）を読むこと。

### 前提（新セッション開始時に確認）

- `git fetch origin main` で remote-tracking ref を更新してから
  `git log --oneline origin/main` を確認し、先頭付近に
  「Merge pull request #3: feat: trade-history に --all-pairs / --year を追加」が見えること
- **旧セッション由来のコミット SHA（7697564 / 06130b7 / a313997 等）はフォーク移行時に
  作り直されており存在しない**。過去作業の照合はコミットメッセージで行う
- **AI エージェントセッションに限り**、git identity 未設定なら
  user.email=noreply@anthropic.com / user.name=Claude を設定する
  （人間のコントリビュータは自身の identity をそのまま使う）
- `npm ci` 後、ローカル hook（lefthook pre-commit）が緑であること。CI（`ci.yml`）は
  hook 経由ではなく共通の 3 検証を直接実行する:
  `npx biome check cli/` / `npx tsc --noEmit` / `npx vitest run`。
  CI はこれに加えて `npm audit --audit-level=critical` を実行するが、
  こちらは警告のみの非ブロッキング（merge gate は上記 3 つ）

### 実装済み（Week 2 ①〜⑥）

- **① JST 年境界ヘルパー**（`cli/date-utils.ts`）:
  `jstYear(ms)` / `jstYearRangeMs(year)→{startMs,endMs}`（半開区間）/ `jstIso(ms)`。
  epoch を +9h ずらして getUTC* で読み TZ 非依存（x13 と同じ安定性をテスト）。
  税務の年分は JST（ADR-004 の例外）。テスト `cli/__tests__/date-utils-jst.test.ts`
- **② deposit-history に `--all` / `--year`**（`cli/commands/private/deposit-history-all.ts`）:
  後方 end 走査で全件ページング・uuid dedup・max-pages 安全弁。
  `--year=<YYYY>` は jstYearRangeMs で範囲を絞り jstYear で厳密フィルタ（end 境界の
  含む/排他に依存しない）。--year は --all を含意し --since/--end と併用不可。出力は
  found_at 昇順。schema def / agents カタログ / completion 更新済み。
  テスト `cli/__tests__/private/deposit-history-all.test.ts`
- **③ withdrawal-history に `--all` / `--year`**（`cli/commands/private/withdrawal-history-all.ts`,
  fork PR #1 merged）: ② のミラー。差分は 2 点 — tsKey は `requested_at`、
  **asset 必須**（ページング前に `AssetSchema` で fail-fast。`trade-history-all` の
  pair 検証と同型）。CodeRabbit（ASSERTIVE）指摘ゼロで通過。
  テスト `cli/__tests__/private/withdrawal-history-all.test.ts`
- **④ trade-history に `--all-pairs` / `--year`**（fork PR #3 merged）: pairs マスタ
  （delist 込み・実機 #4）の全 `name` を逐次ループし各 pair の `tradeHistoryAll` を
  マージ。構成は `trade-history-dispatch.ts`（振り分け）→ `trade-history-all-pairs.ts`
  （横断取得）→ `trade-history-all.ts` → leaf の一方向依存（ディスパッチャは
  dispatch ファイルへ移動し、`trade-history-all.ts` は 100 行以内に復帰）。要点:
  - dedup は `pair:trade_id` 複合キー（trade_id の pair 横断一意性が未確認のため
    安全側）。出力は `executed_at` 昇順。`--year` は ②③ と同仕様で、
    `--pair` 併用時は単一ペアの年分、`--all-pairs` 併用時は全ペアの年分
  - `--max-pages`（pair ごと上限）到達ペアは `meta.truncatedPairs` で報告
  - `--max-pages` / `--year` の検証は `input-schemas.ts` の `parseMaxPages` /
    `resolveYearWindow` に共通化（②③ も同ヘルパーへ移行済み）。YearSchema は
    `^[1-9]\d{3}$`（0 始まり 4 桁年は Date.UTC の 1900 年代補正で範囲と
    フィルタが食い違うため拒否。CodeRabbit 指摘）
  - レート制限は逐次実行 + `http-core` の `waitForSlot` / 429 リトライで対応
    （追加実装なしを確認）。進捗の stderr 出力は既存 `--all` 系との一貫性を理由に
    見送り（CodeRabbit が learning として記録済み）
  - テスト `cli/__tests__/private/trade-history-all-pairs.test.ts` /
    `trade-history-dispatch.test.ts`
- **⑤ error-catalog の 50009**: 「注文照会は pair × order_id の複合キーで、
  不一致でも 50009（実機 #2）」を `scripts/gen-agents-catalog.ts` の GENERAL
  `agent_action` に反映して regenerate。コンパニオン文書
  `skills/_shared/references/error-catalog.md` §3b にも同旨を追記
- **⑥ withdrawal-history に `--all-assets` / `--year`**（本 PR）: ③ の派生として残って
  いた「asset 一覧の取得元」判断は、ユーザーに確認のうえ **pairs マスタの
  `base_asset ∪ quote_asset`**（delist 込み）を採用（private `assets` は「口座が
  過去に触れた asset 全量」を返すか未確認で、delist・リネーム前シンボルが
  落ちるリスクがあったため見送り）。④ と同型の一方向依存
  （`withdrawal-history-dispatch.ts` → `withdrawal-history-all-assets.ts` →
  `withdrawal-history-all.ts` → leaf）。要点:
  - dedup は `asset:uuid` 複合キー（④ の `pair:trade_id` と同じ安全側の判断。
    uuid 自体は asset 内で一意なので実質的には安全弁）
  - `--max-pages`（asset ごと上限）到達 asset は `meta.truncatedAssets` で報告
    （`types.ts` の `ResultMeta` に追加）
  - `--all-assets` は `--asset` と併用不可。`--year` は `--asset` 併用時は単一 asset の
    年分、`--all-assets` 併用時は全 asset の年分（④ と同仕様）
  - dispatch の引数型は leaf（`WithdrawalHistoryArgs`）の `asset` が必須型なのに対し
    `--all-assets` 時は asset 省略が正規入力になるため、`asset?: string` に緩めた
    別型を定義（trade 側は `pair` がそもそも leaf で `.optional()` なのでこの問題が
    出ない。withdrawal 固有の型差分）
  - テスト `withdrawal-history-all-assets.test.ts` / `withdrawal-history-dispatch.test.ts`
    （trade-history-all-pairs.test.ts / trade-history-dispatch.test.ts のミラー）

### 次タスク（Week 3〜）

- Week 2 の生データ取得網羅（①〜⑥）はこれで完了。以降は Week 3（ヒアリング実施・
  トラック 3 設計）と Week 4（正規化仕様）へ

### 実装 gotchas（chaos 規約）

- Result パターン（throw 禁止 x01）、Zod が型の単一ソース、`--format=json|table|csv`
- **1 ファイル 100 行**（x04）。超過は冒頭に `// 100行超: <理由>` コメント
- handler description / schema def を変えたら **`npx tsx scripts/gen-agents-catalog.ts`**
  （x17 drift）+ completion スナップショット更新
  **`npx vitest run cli/__tests__/completion/scripts.test.ts -u`**
- private テストは実 API を叩かずモック（`mockFetchData` / `mockFetchDataCapture` /
  `__fixtures__/private/`）

### CI / レビュー基盤（2026-07-24 時点）

- fork の GitHub Actions は有効化済み。`ci.yml`（biome + tsc + vitest +
  npm audit critical）は PR / main push で走る
- **`security.yml`（Security Audit）は fork では `disabled_fork` になり得る**: schedule
  トリガーを含む workflow は fork では per-workflow で無効化され、push / pull_request
  トリガーまで全て止まる仕様。Actions タブ → Security Audit → 「Enable workflow」
  （オーナー操作）で解除する。本 fork は有効化済みで、base=main の PR では
  `audit`（npm audit high・ブロッキング）+ `gitleaks` が走る。
  走っていなければユーザーに有効化を依頼する
- CodeRabbit は base=main の PR 作成で自動レビュー。手動トリガーは
  `@coderabbitai review` コメント

### 段階3（Week 6）着手前に決める設計

- **P-02 内部 Decimal 非丸め・浮動小数点禁止**（tax-research §3）→ JS number 不可。
  decimal ライブラリ or BigInt 固定小数点の自作を **Week 6 冒頭に ADR 化**。丸めは
  付録 D の互換モードで再現、FAQ 設例をゴールデンテスト
- **信用の二重計上禁止**: 実現損益は決済レコードの `profit_loss`（ネット値）を採用し、
  fee / interest を再控除しない（実機 #2）
