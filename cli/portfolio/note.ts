// 出力に必ず載せる前提の注記。**移植元**は `bitbankinc/bitbank-lab-mcp` の
// `src/handlers/portfolio/calc.ts#PERFORMANCE_NOTE`（`ecf05ae` 時点）。
//
// 注記をデータ側に持つ理由: skill / LLM 側で書き忘れると「現在の保有量 × 過去の価格」
// との違いが読み手に伝わらず、実機確認 #14 の追試で起きた誤読（資産が半減したように
// 見える）が復活する。前提はデータに同梱して初めて剥がれない。

export const RECONSTRUCTION_NOTE =
  "各時点の保有は現在の残高から約定・入出金を逆算して復元し、その時点の始値（1day 足 open）で評価。" +
  "candle が取れない資産は現在価格で代替。暗号資産の入出庫は現在価格で仮評価。" +
  "純入出金は元本移動のみ（出金手数料を含まない）。" +
  "調整後増減 = 単純増減 − 純入出金（市場変動 + 出金手数料コスト）。";

/** note を機械可読に割った版。skill / LLM が前提を 1 つずつ引用できるようにする。 */
export const RECONSTRUCTION_ASSUMPTIONS: readonly string[] = [
  "保有は現在の残高から約定・入出金を逆算して復元したもので、当時の残高スナップショットではない",
  "評価は各時点の 1day 足 open（UTC 日境界）。取得できない資産は現在価格で代替する",
  "暗号資産の入出庫は現在価格で仮評価する（当時の価格ではない）",
  "入出金は status=DONE のみを反映する（未確定・キャンセルは残高を動かしていない）",
  "現物のみ。信用約定は巻き戻さない（現物残高を動かさないため）。実現損益は JPY 残高を動かすので、期間内に信用決済があると過去の点がずれる",
  "販売所（即時売買）の取引は API に現れないため反映されない",
  "最終点は復元値ではなく現在の実測評価額（ticker last × 残高）",
];
